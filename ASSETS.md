# Assets Manifest — Huber Capital

Images live in `public/assets/` and are referenced by absolute path (e.g. `/assets/hero.jpg`).
Current files are **temporary placeholders** (reused from the old site) and should be replaced with
real photography before launch. Keep the same filenames to avoid code changes.

| File | Used on | Purpose | Recommended size | Status |
|------|---------|---------|------------------|--------|
| `hero.jpg` | Home + page heroes | Dubai skyline / signature shot | 2000×1200 landscape | placeholder |
| `about.jpg` | Home + About | **Gilad Huber portrait** (ideal) or Dubai interior | 1200×1500 portrait (4:5) | placeholder |
| `og.jpg` | Social share preview | Branded Open-Graph image | 1200×630 | placeholder (copy of hero) |
| `favicon.svg` | Browser tab | HC monogram | vector | done (typographic) |

## Optional, later
- Per-area photos for Projects & Areas (Palm Jumeirah, Downtown, Dubai Marina, Business Bay, JVC) — currently elegant text cards.
- A real logo file (otherwise the typographic HC monogram + wordmark is used).
- A short muted drone video for the hero (even more premium; image is the fallback).

## Notes
- Compress before adding (WebP/AVIF or optimized JPG), aim < ~400 KB each.
- Give images meaningful Hebrew `alt` text.
